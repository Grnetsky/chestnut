{
  "name": "chestnut",
  "version": "0.0.1",
  "description": "chestnut",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r exec rm -rf dist node_modules",
    "dev": "pnpm -r --parallel dev"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
